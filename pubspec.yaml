name: diagnostics_getwidget
description: "Implementation of Azure Portal Extensions Dashboard in Flutter with GetWidget"
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ^3.9.2
dependencies:
  flutter:
    sdk: flutter
  getwidget: ^7.0.0
  http: ^1.5.0
  flutter_riverpod: ^2.5.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.3
  coverage: ^1.0.0
flutter:
  uses-material-design: true
